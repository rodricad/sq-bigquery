'use strict';

const TableConst = {
    Schema: {
        Type: {
            STRING: 'STRING',
            INTEGER: 'INTEGER',
            FLOAT: 'FLOAT',
            BOOLEAN: 'BOOLEAN',
            TIMESTAMP: 'TIMESTAMP'
        },
        Mode: {
            NULLABLE: 'NULLABLE'
        }
    }
};

module.exports = TableConst;
